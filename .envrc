# Automatically load the Nix shell environment
use nix

# Export additional environment variables if needed
export LEAN_PATH=""

# Inform the user about the development environment
echo "🔧 Lean 4 development environment loaded via direnv"